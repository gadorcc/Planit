<nav class="navbar navbar-expand-lg navbar-light bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to plans_path, class: "navbar-brand" do %>
      <%= image_tag "planit.logo.pg.svg" %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar">
      <span class="navbar-toggler-icon"></span>
      <% if user_signed_in? %>
        <% if current_user.avatar_picture.present? %>
            <%= cl_image_tag(current_user.avatar_picture.key, class: "ms-2", width: 30, height: 30, crop: :fill, gravity: :face) %>
        <% else %>
        <%= image_tag "avatarhome1.png", alt: "Avatar", class: "ms-2" %>
      <% end %>
      <% end %>
    </button>
    <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasNavbar" aria-labelledby="offcanvasNavbarLabel">
      <div class="offcanvas-header">
        <h5 class="offcanvas-title" id="offcanvasNavbarLabel"><%= image_tag "planitwhite-logo.svg" %></h5>
        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
      </div>
      <div class="offcanvas-body">
        <ul class="navbar-nav justify-content-end flex-grow-1 pe-3 mb-3">
          <% if user_signed_in? %>
  <li class="nav-item">
    <div class="account-section mt-4">
      <h4 class="header">
        Account
      </h4>
      <a class="nav-link" href="<%= edit_user_registration_path %>">
        Profile
      </a>
    </div>
  </li>
   <li class="nav-item">
    <a class="nav-link" href="/plans">Plans</a>
  </li>
  <li class="nav-item">
    <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "nav-link" %>
  </li>
</ul>

<% else %>
  <li class="nav-item me-3">
    <%= link_to "Login", new_user_session_path, class: "nav-link" %>
  </li>

<li class="nav-item">
<%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
</li>
<% end %>
</ul>
</div>
</div>

  </div>
</nav>
