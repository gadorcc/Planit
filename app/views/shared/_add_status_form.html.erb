<%= simple_form_for([@plan, @current_participant]) do |f| %>
   <%=f.input :status, as: :hidden, input_html: {value: "Going"}  %>
   <%= submit_tag "Going", class: "btn btn-primary" %>
 <% end %>

 <%= simple_form_for([@plan, @current_participant]) do |f| %>
   <%=f.input :status, as: :hidden, input_html: {value: "Maybe"}  %>
   <%= submit_tag "Maybe", class: "btn btn-primary" %>
 <% end %>

 <%= simple_form_for([@plan, @current_participant]) do |f| %>
   <%=f.input :status, as: :hidden, input_html: {value: "Not\Going"}  %>
   <%= submit_tag "Not Going", class: "btn btn-primary" %>
 <% end %>
