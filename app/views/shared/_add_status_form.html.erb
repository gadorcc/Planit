<%= simple_form_for([@plan, @current_participant]) do |f| %>

    <%=f.input :status, collection: Participant::STATUSES, label: false, input_html: { data: {
      dropdown_target: "status",
      action: "change->dropdown#changeStatus"
  }} %>
<% end %>
