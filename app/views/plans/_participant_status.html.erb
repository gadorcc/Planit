<small class="d-inline-flex mb-1 px-2 py-1">
          <% if participant.user.avatar_picture.present? %>
            <%= cl_image_tag(participant.user.avatar_picture.key, alt: 'Yes', width: 42, height: 42, class: participant.status) %>
          <% else %>
            <%= image_tag "avatarhome1.png", alt: "Avatar", width: 42, height: 42, class: participant.status %>
          <% end %>
      </small>
