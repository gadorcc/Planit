<h1>Plans#index</h1>
<p>Find me in app/views/plans/index.html.erb</p>
<h1>Upcoming events</h1>
  <% @plans.each do |plan| %>
    <h2><%= plan.title %></h2>
    #if end day does not equal start day display start_datetime and end_datetime
      <p><%= plan.start_datetime.strftime('%a%-d%b%1%P') %> - <%= plan.end_datetime.strftime('%a%-d%b%1%P') %></p>
      <%# <p><%= plan.start_datetime.strftime('%a%-d%b%1%P') %>
      <%# <%= plan.end_datetime.strftime('%1%P') %>
      <p><%= plan.description %></p>
     #status
      #messages
      <% end %>
    <% @plans.each do |plan| %>
      <h2><%= plan.title %></h2>

<h1>Make a new plan</h1>
